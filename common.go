package goswag

const (
	// These are the types that are used to define the type of the field in the swagger.
	StringType = "string"
	IntType    = "int"
	NumberType = "number"
	BoolType   = "boolean"
)
